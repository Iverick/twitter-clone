<div class="hashtag p-3" data-controller="hashtag" data-url="<%= hashtag_path(hashtag.id) %>">
  <p class="fw-bold fs-5 mb-0">#<%= hashtag.tag %></p>
  <p class="text-muted mb-0"><%= hashtag.tweets.size %> tweets</p>
</div>
