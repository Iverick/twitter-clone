<div class="col-6">
  <div class="mb-5">
    <div class="d-flex flex-row align-items-center">
      <%= link_to :back, class: "me-4 text-decoration-none text-black" do %>
        <h3 class="mb-0"><i class="bi bi-arrow-left"></i></h3>
      <% end %>
      <div>
        <h4 class="mb-0"><%= @user.username %></h4>
        <span class="fs-6 text-muted"><%= @user.tweets.count %> Tweets</span>
      </div>
    </div>
  </div>

  <%= render "users/profile_section", user: @user %>

  <%= render "users/tweets_collection", tweet_presenters: @tweets %>
</div>

<div class="col">
  Column
</div>

<% if controller_name == "profile" %>
  <!-- Edit Profile Modal -->
  <div class="modal" id="profile-edit-modal" tabindex="-1" aria-labelledby="profile-edit-modal-label" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header border-bottom-0">
          <div class="d-flex justify-content-start align-items-center">
            <button type="button" class="close-modal-btn btn-close me-4" data-bs-dismiss="modal" aria-label="Close">
            </button>

            <h4 class="mb-0">Edit profile</h4>
          </div>
        </div>
        <div class="modal-body">
          <%= render "users/profile_form" %>
        </div>
      </div>
    </div>
  </div>
<% end %>
