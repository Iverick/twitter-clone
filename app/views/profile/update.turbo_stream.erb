<%= turbo_stream.replace "profile-section" do %>
  <%= render partial: "users/profile_section", locals: { user: current_user } %>
<% end %>

<%= turbo_stream.replace "tweets-collection" do %>
  <%= render partial: "users/tweets_collection", locals: { tweet_presenters: @tweets, as: :tweet_presenter } %>
<% end %>
