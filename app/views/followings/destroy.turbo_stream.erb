<%= turbo_stream.replace "follow-unfollow-button" do %>
  <%= render partial: "users/follow_button", locals: { user: @user, current_user: current_user } %>
<% end %>

<%= turbo_stream.replace "profile-section" do %>
  <%= render partial: "users/profile_section", locals: { user: @follower } %>
<% end %>
